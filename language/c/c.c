#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
	/* 文件操作 */
	// 修改文件的名字
	// rename("test.txt", "test1.txt");
	//删除文件
	// remove("test1.txt");
	// 创建符号链接
	// symlink("c.c", "c.c.link");
	//切换目录
	// // if (chdir("/home/wuming") < 0)
	// // {
	// // 	printf("目录切换失败");
	// // }
	// // char *currentworkdir;
	// // size_t size;
	// // currentworkdir=malloc(100);
	// // getcwd(currentworkdir,size);
	// printf("当前目录是%s\n",currentworkdir);


	return 0;
}
